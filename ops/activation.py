"""
ops/activation.py — 激活函数算子接口
"""

# silu(x):
#     """SiLU (Swish): x * sigmoid(x)"""

# gelu(x):
#     """GELU: 0.5 * x * (1 + tanh(sqrt(2/pi) * (x + 0.044715 * x^3)))"""

# softmax(x, dim):
#     """Softmax: exp(x - max) / sum(exp(x - max))"""
